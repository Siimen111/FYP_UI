# VTrack Desktop (Electron)

## Setup

```bash
cd C:\Users\sherm\FYP\desktop
npm install
```

## Run

```bash
npm start
```

## Notes

- Ensure the FastAPI server is running (default: http://127.0.0.1:8000).
- Update the API Base URL in the app if your server is bound to a different host/port.
